# Scope (Prototype)

- Browser-only prototype.
- No backend, no server, no database.
- Data is stored as JSON in browser storage (default: `localStorage`).

## Definition of Done
A task is done when:
- It works after refresh.
- There are no console errors.
- Expected state persists after reload.
- UI/UX remains consistent with the chosen UI library.
